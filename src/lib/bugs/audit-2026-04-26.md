# Auditoría de bugs — Petfhans Platform
**Fecha:** 2026-04-26  
**Revisado por:** Claude Code (análisis estático + lectura de archivos)  
**Rama:** `claude/load-supabase-mcp-BxHFl`  
**Método:** revisión manual de todos los archivos en `src/app/api/`, `src/lib/`, `src/middleware.ts` y componentes críticos

---

## 🔴 CRÍTICO — riesgo de runtime crash o bypass de seguridad

---

### BUG-001 — `pets/upload-photo/route.ts` — bypass de acceso + crash

**Archivo:** `src/app/api/pets/upload-photo/route.ts`  
**Líneas:** 13–14, 26, 36, 58  
**Severidad:** 🔴 Crítico  

**Descripción:**  
`profile` se obtiene con `.single()` pero nunca se verifica si es `null`. Los checks de acceso usan optional chaining (`profile?.role`), lo que hace que si `profile` es `null`, **ninguna verificación de ownership corre**. El código llega hasta la línea 58 donde `profile!.id` causa un runtime crash.

**Código problemático:**
```typescript
const { data: profile } = await supabase.from('profiles')
  .select('id, role, clinic_id').eq('user_id', user.id).single()
// No hay null check aquí ↓

if (profile?.role === 'pet_owner') {        // si profile=null → se salta
  // ... verificación de pet_access
}
if (profile?.role && ['vet_admin',...]) {   // si profile=null → se salta
  // ... verificación de clinic ownership
}
// Ambos checks se saltan → cualquier usuario sin perfil en BD llega aquí:
uploaded_by: profile!.id,   // línea 58 → ReferenceError: cannot read 'id' of null
```

**Impacto:** Un usuario autenticado cuyo `profiles` row no existe puede llegar al upload sin pasar ninguna verificación de acceso. El crash en línea 58 evita el daño pero es un comportamiento incorrecto.

**Fix:**
```typescript
const { data: profile } = await supabase.from('profiles')
  .select('id, role, clinic_id').eq('user_id', user.id).single()
if (!profile) return NextResponse.json({ error: 'Perfil no encontrado' }, { status: 403 })
```

---

### BUG-002 — `appointments/emergency/route.ts` — `profile!.id` sin null guard

**Archivo:** `src/app/api/appointments/emergency/route.ts`  
**Líneas:** 18–22, 44  
**Severidad:** 🔴 Crítico  

**Descripción:**  
`profile` se obtiene dentro de un `Promise.all` junto con `pet` y `vet`. Hay verificaciones para `pet` y `vet` (líneas 24–25), pero **no para `profile`**. En línea 44 se usa `profile!.id` con non-null assertion, que provoca runtime crash si el usuario no tiene fila en `profiles`.

**Código problemático:**
```typescript
const [{ data: profile }, { data: pet }, { data: vet }] = await Promise.all([
  supabase.from('profiles').select('id, full_name').eq('user_id', user.id).single(),
  admin.from('pets').select(...).eq('id', pet_id).single(),
  admin.from('profiles').select(...).eq('id', vet_id).single(),
])

if (!pet)  return NextResponse.json({ error: 'Mascota no encontrada' }, { status: 404 })
if (!vet)  return NextResponse.json({ error: 'Veterinario no encontrado' }, { status: 404 })
// ← falta: if (!profile) return 403

// ... líneas después:
owner_id: profile!.id,   // línea 44 → crash si profile = null
```

**Fix:**
```typescript
if (!profile) return NextResponse.json({ error: 'Perfil no encontrado' }, { status: 403 })
if (!pet)     return NextResponse.json({ error: 'Mascota no encontrada' }, { status: 404 })
if (!vet)     return NextResponse.json({ error: 'Veterinario no encontrado' }, { status: 404 })
```

---

## 🟠 ALTO — lógica incorrecta o funcionalidad rota

---

### BUG-003 — `appointments/route.ts` — ternario muerto en envío de emails a vets

**Archivo:** `src/app/api/appointments/route.ts`  
**Línea:** 122  
**Severidad:** 🟠 Alto  

**Descripción:**  
El ternario que construye el campo `to:` del email tiene **ambas ramas idénticas**. Esto es un error de copy-paste de IA: en ningún caso el `to:` cambia según el número de veterinarios. La lógica tenía intención de variar el comportamiento pero se dejó incompleta.

**Código problemático:**
```typescript
to: vetEmails.length === 1 ? vetEmails[0] : vetEmails[0],  // ← ambas ramas = vetEmails[0]
cc: vetEmails.slice(1).length ? vetEmails.slice(1) : undefined,
```

**Impacto:** El comportamiento real es correcto por accidente (`to` = primer vet, `cc` = resto), pero el ternario es dead code que confunde a quien lo lee y podría ocultartar un bug si alguien "lo completa" incorrectamente.

**Fix:**
```typescript
to: vetEmails[0],
cc: vetEmails.length > 1 ? vetEmails.slice(1) : undefined,
```

---

### BUG-004 — `appointments/route.ts` — `.single()` en check de slot disponible

**Archivo:** `src/app/api/appointments/route.ts`  
**Líneas:** 42–50  
**Severidad:** 🟠 Alto  

**Descripción:**  
Se usa `.single()` para verificar si un slot ya está reservado. `.single()` devuelve `{ data: null, error }` tanto cuando hay **cero filas** (slot libre) como cuando hay **múltiples filas** (datos corruptos / race condition). En ambos casos `data` es `null`, por lo que el código interpreta incorrectamente "múltiples citas en el mismo slot" como "slot libre" y permite la reserva.

**Código problemático:**
```typescript
const { data: existing } = await admin.from('appointments')
  .select('id')
  .eq('clinic_id', pet.clinic_id)
  .eq('appointment_date', appointment_date)
  .eq('appointment_time', appointment_time)
  .in('status', ['pending', 'confirmed'])
  .single()                      // ← incorrecto para este caso de uso

if (existing) return NextResponse.json({ error: 'Ese horario ya está reservado' }, { status: 409 })
```

**Fix:**
```typescript
const { data: existing } = await admin.from('appointments')
  .select('id')
  .eq('clinic_id', pet.clinic_id)
  .eq('appointment_date', appointment_date)
  .eq('appointment_time', appointment_time)
  .in('status', ['pending', 'confirmed'])
  .limit(1)                      // ← correcto: devuelve array

if (existing && existing.length > 0)
  return NextResponse.json({ error: 'Ese horario ya está reservado' }, { status: 409 })
```

---

### BUG-005 — `care-requests/[id]/route.ts` — acción `accept` devuelve 501

**Archivo:** `src/app/api/care-requests/[id]/route.ts`  
**Líneas:** 45–51  
**Severidad:** 🟠 Alto  

**Descripción:**  
La acción más importante del flujo de vinculación dueño-clínica (aceptar una solicitud de cuidado) no está implementada y devuelve HTTP 501. El endpoint existe, el frontend lo llama, pero la funcionalidad core está diferida indefinidamente ("Deferred to Fase D").

**Código problemático:**
```typescript
if (action === 'accept') {
  // Deferred to Fase D — requires profile_clinics.insert()
  return NextResponse.json(
    { error: 'La vinculación automática estará disponible en la próxima versión' },
    { status: 501 }
  )
}
```

**Acciones `reject` y `block` sí están implementadas.** Solo `accept` falta.

**Lo que debe hacer `accept`:**
1. Insertar registro en `profile_clinics` (vincular el dueño a la clínica)
2. Actualizar `care_requests.status = 'accepted'`
3. Notificar al dueño por email
4. Opcionalmente, transferir las mascotas del dueño a la clínica

---

### BUG-006 — `marketplace/clinics/route.ts` — búsqueda sin límite de resultados

**Archivo:** `src/app/api/marketplace/clinics/route.ts`  
**Línea:** 59  
**Severidad:** 🟠 Alto  

**Descripción:**  
La query de búsqueda de clínicas no tiene `.limit()`. Si hay miles de clínicas en el sistema, la respuesta puede ser masiva, afectando memoria del servidor y tiempo de respuesta del cliente.

**Código problemático:**
```typescript
let query = admin
  .from('clinics')
  .select('id, name, slug, verified, public_profile')
  .order('name')
// No hay .limit() ↑

const { data: clinics, error } = await query   // retorna TODO sin límite
```

**Fix:**
```typescript
const { data: clinics, error } = await query.limit(50)
```
Con paginación adecuada si se necesita scroll infinito.

---

### BUG-007 — `care-requests/route.ts` y `care-requests/[id]/route.ts` — `profile.clinic_id` obsoleto (Phase B)

**Archivos:** `src/app/api/care-requests/route.ts`, `src/app/api/care-requests/[id]/route.ts`  
**Líneas:** varios  
**Severidad:** 🟠 Alto (bloqueante en Phase B)  

**Descripción:**  
Ambas rutas leen `clinic_id` directamente de `profiles` (patrón Phase A — scalar). Cuando se ejecute la migración `019_nullify_profiles_clinic_id.sql`, este campo quedará `NULL` para todos los usuarios, y estas rutas devolverán 403 a todos los `vet_admin`.

**Código problemático (`[id]/route.ts` líneas 16–25):**
```typescript
const { data: profile } = await admin
  .from('profiles')
  .select('id, role, clinic_id')    // ← clinic_id será NULL post-019
  .eq('user_id', user.id)
  .single()

if (!profile.clinic_id) {
  return NextResponse.json({ error: 'Sin clínica asignada' }, { status: 403 })
  // ← todos los vet_admin llegarán aquí tras migración 019
}
```

**Fix (Phase B):** Leer la clínica activa desde el header `x-active-clinic-id` (inyectado por middleware desde la cookie), igual que el resto de rutas migradas en B3.

```typescript
const activeClinicId = req.headers.get('x-active-clinic-id')
if (!activeClinicId) return NextResponse.json({ error: 'Sin clínica activa' }, { status: 403 })
```

---

## 🟡 MEDIO — comportamiento incorrecto o inseguro no crítico

---

### BUG-008 — `appointments/slots/route.ts` — enumeración de slots sin verificar ownership

**Archivo:** `src/app/api/appointments/slots/route.ts`  
**Líneas:** 11–14  
**Severidad:** 🟡 Medio  

**Descripción:**  
Cualquier usuario autenticado puede consultar los slots disponibles de **cualquier clínica** del sistema pasando un `clinic_id` arbitrario. No se verifica que el usuario tenga relación con esa clínica (sea dueño de una mascota vinculada, o staff).

**Código problemático:**
```typescript
const clinic_id = searchParams.get('clinic_id')  // dato del cliente — no verificado
const date      = searchParams.get('date')
```

**Impacto:** Endpoint de enumeración abierto. Un atacante puede mapear la disponibilidad de cualquier clínica sin estar relacionado con ella.

---

### BUG-009 — `appointments/route.ts` / `appointments/[id]/route.ts` — emails silenciados sin logging

**Archivos:** `src/app/api/appointments/route.ts` (líneas 109, 146), `src/app/api/appointments/[id]/route.ts` (línea 114)  
**Severidad:** 🟡 Medio  

**Descripción:**  
Todos los envíos de email usan `.catch(() => {})` — el error se descarta completamente. Si Resend falla (rate limit, clave expirada, dominio no verificado), ni el usuario ni el sistema se enteran.

**Código problemático:**
```typescript
await resend.emails.send({ ... }).catch(() => {})   // error invisible
```

**Fix mínimo:**
```typescript
await resend.emails.send({ ... }).catch((err) => {
  console.error('[Resend] fallo al enviar email:', err?.message)
})
```

---

### BUG-010 — `vet/ai-chat/route.ts` — registros médicos sin ordenar por fecha

**Archivo:** `src/app/api/vet/ai-chat/route.ts`  
**Severidad:** 🟡 Medio  

**Descripción:**  
Los registros médicos incluidos en el contexto del chat de IA se obtienen con `.limit(10)` pero sin `.order('visit_date', { ascending: false })`. Sin ordenación explícita, Supabase no garantiza el orden, y el modelo puede recibir registros viejos/irrelevantes en lugar de los más recientes.

**Fix:**
```typescript
.order('visit_date', { ascending: false })
.limit(10)
```

---

## 🔵 BAJO — calidad de código / deuda técnica

---

### BUG-011 — URLs de dominio hardcodeadas en múltiples rutas

**Archivos:**
- `src/app/api/vet/create-invitation/route.ts` — `https://petfhans.com/auth/invite`
- `src/app/api/vet/resend-invitation/route.ts` — `https://petfhans.com/auth/invite`
- `src/app/api/appointments/[id]/route.ts` — `https://meet.jit.si/` y `https://petfhans.com/owner/dashboard`
- `src/app/api/appointments/route.ts` — `https://petfhans.com/owner/dashboard`, `https://petfhans.com/vet/appointments`

**Descripción:** Si el dominio cambia o hay entorno de staging, hay que buscar y reemplazar manualmente en múltiples archivos. Deben usar `process.env.NEXT_PUBLIC_APP_URL`.

---

### BUG-012 — `marketplace/clinics/route.ts` — columna `public_profile` seleccionada pero ignorada

**Archivo:** `src/app/api/marketplace/clinics/route.ts`  
**Línea:** 50  
**Severidad:** 🔵 Bajo  

**Descripción:**  
La columna `public_profile` se incluye en el `select()` pero nunca se usa en lógica de negocio ni se retorna al cliente de forma diferenciada. Aumenta el payload innecesariamente.

**Código:**
```typescript
.select('id, name, slug, verified, public_profile')   // public_profile: fetched, never used
```

---

## 🔴 CRÍTICO — nuevos (audit 2026-04-26 v2)

---

### BUG-013 — `files/upload/route.ts` — `profile!.id` sin null guard

**Archivo:** `src/app/api/files/upload/route.ts`
**Línea:** 58
**Severidad:** 🔴 Crítico

**Descripción:**
Mismo patrón que BUG-001 pero en la ruta de subida de archivos. `profile` se obtiene con optional chaining (`profile?.role`) pero nunca se verifica si es `null`. En la línea 58, `profile!.id` provoca runtime crash si el usuario no tiene fila en `profiles`.

**Código problemático:**
```typescript
const { data: profile } = await supabase.from('profiles')
  .select('id, role').eq('user_id', user.id).single()

if (profile?.role === 'pet_owner') {   // si profile=null → se salta
  // ...
}
// Ambos guards saltan si profile es null — llega aquí:
uploaded_by: profile!.id,   // línea 58 → crash
```

**Fix:**
```typescript
if (!profile) return NextResponse.json({ error: 'Perfil no encontrado' }, { status: 403 })
```

---

## 🟠 ALTO — nuevos

---

### BUG-014 — `owner/setup/route.ts` — escribe `profiles.clinic_id` obsoleto post-019

**Archivo:** `src/app/api/owner/setup/route.ts`
**Líneas:** 15, 19, 27
**Severidad:** 🟠 Alto (bloqueante post-migración 019)

**Descripción:**
La ruta lee `profile.clinic_id` de la tabla `profiles` y luego escribe sobre ese mismo campo al vincular al dueño con una clínica. Tras la migración `019_nullify_profiles_clinic_id.sql`, el campo siempre será `NULL` y tanto la lectura como la escritura dejarán de funcionar. La vinculación debe hacerse via `profile_clinics`.

**Código problemático:**
```typescript
const { data: profile } = await admin.from('profiles')
  .select('id, role, clinic_id').eq('user_id', user.id).single()

let clinicId = profile.clinic_id   // será NULL post-019

if (clinic_slug && !clinicId) {
  // ...
  await admin.from('profiles').update({ clinic_id: clinicId }).eq('id', profile.id)
  // ↑ escribe en campo nullificado post-019 — silenciosamente no hace nada
}
```

**Fix:**
```typescript
// Leer clínica activa desde profile_clinics en lugar de profiles.clinic_id
// Escribir en profile_clinics al vincular:
await admin.from('profile_clinics').upsert(
  { user_id: profile.user_id, clinic_id: clinicId, role: 'pet_owner' },
  { onConflict: 'user_id,clinic_id', ignoreDuplicates: true }
)
```

---

### BUG-015 — `marketplace/vets/route.ts` — búsqueda sin `.limit()`

**Archivo:** `src/app/api/marketplace/vets/route.ts`
**Línea:** 20 (ausencia de límite)
**Severidad:** 🟠 Alto

**Descripción:**
Misma clase que BUG-006 pero para la ruta de veterinarios. La query retorna todos los perfiles con rol `vet_admin` o `veterinarian` del sistema sin ningún límite. El archivo completo tiene solo 29 líneas sin `.limit()`.

**Código problemático:**
```typescript
let query = admin
  .from('profiles')
  .select('id, full_name, avatar_url, role, clinic_id, clinics(id, name, slug)')
  .in('role', ['vet_admin', 'veterinarian'])
  .not('clinic_id', 'is', null)
  .order('full_name')
  // ← sin .limit()

const { data: vets, error } = await query   // retorna TODO sin límite
```

**Fix:**
```typescript
const { data: vets, error } = await query.limit(100)
```

---

### BUG-016 — `clinic-join-requests/[id]/route.ts` — compat Phase A escribe `profiles.clinic_id` post-019

**Archivo:** `src/app/api/clinic-join-requests/[id]/route.ts`
**Líneas:** 64–68
**Severidad:** 🟠 Alto (deuda técnica bloqueante post-019)

**Descripción:**
El bloque de aceptación de solicitudes de vet incluye un bloque "Phase A compat" que escribe `profiles.clinic_id`. Este bloque fue intencional mientras se migraba, pero con la migración `019_nullify_profiles_clinic_id.sql` ya ejecutada (o pendiente), el campo está siendo nullificado. El bloque debe eliminarse — `profile_clinics` ya lo maneja correctamente en las líneas anteriores.

**Código problemático:**
```typescript
// Phase A compat: also update profiles.clinic_id until migration 019 runs
await admin
  .from('profiles')
  .update({ clinic_id: joinRequest.clinic_id })
  .eq('id', joinRequest.vet_id)
// ↑ Silenciosamente no tiene efecto post-019 y genera confusión
```

**Fix:** Eliminar este bloque completamente. `profile_clinics.upsert` en las líneas anteriores es suficiente.

---

## 🟡 MEDIO — nuevos

---

### BUG-018 — `appointments/[id]/route.ts` — `callerProfile.clinic_id` potencialmente `null` post-guard

**Archivo:** `src/app/api/appointments/[id]/route.ts`
**Líneas:** 22–43
**Severidad:** 🟡 Medio

**Descripción:**
`callerProfile` se verifica con optional chaining (`callerProfile?.clinic_id`). Si el guard pasa, TypeScript no estrecha el tipo y `callerProfile.clinic_id` sigue siendo `string | null`. En la línea 43 se usa directamente en una comparación de igualdad que fallará silenciosamente si en algún refactor futuro se elimina el guard, o si el campo es `null` por migración 019.

**Código problemático:**
```typescript
if (!callerProfile?.clinic_id) {
  return NextResponse.json({ error: 'Sin clínica asignada' }, { status: 403 })
}
// ... líneas después:
if (!appointmentClinicId || appointmentClinicId !== callerProfile.clinic_id) {
  // callerProfile.clinic_id todavía tipado como string | null aquí
}
```

**Fix:**
```typescript
const activeClinicId = callerProfile?.clinic_id
if (!activeClinicId) return NextResponse.json({ error: 'Sin clínica asignada' }, { status: 403 })

// Usar variable narrowed en vez del campo opcional:
if (!appointmentClinicId || appointmentClinicId !== activeClinicId) {
  return NextResponse.json({ error: 'Sin permisos sobre esta cita' }, { status: 403 })
}
```

---

## 🔵 BAJO — nuevos

---

### BUG-019 — `appointments/route.ts` — non-null assertions `profile!.id`, `profile!.email` tras guard

**Archivo:** `src/app/api/appointments/route.ts`
**Líneas:** 57, 87, 94, 135
**Severidad:** 🔵 Bajo

**Descripción:**
El guard `if (!profile) return 403` existe en línea 24, pero en todo el resto del handler se usa `profile!.id`, `profile!.email` y `profile!.full_name` con non-null assertions. Son seguras en tiempo de ejecución dado el guard, pero el `!` es un patrón frágil: si alguien mueve o elimina el guard, los crashes son silenciosos. TypeScript ya lo sabe — debería poder inferir el tipo sin `!` si la variable se extrae.

**Fix:**
```typescript
// Extraer variables narrowed inmediatamente después del guard:
if (!profile) return NextResponse.json({ error: 'Perfil no encontrado' }, { status: 403 })
const { id: profileId, email: profileEmail, full_name: profileName } = profile

// Usar profileId, profileEmail, profileName en el resto del handler
```

---

### BUG-020 — `care-requests/[id]` y `clinic-join-requests/[id]` — emails sin retry ni escalación

**Archivos:** `src/app/api/care-requests/[id]/route.ts:98`, `src/app/api/clinic-join-requests/[id]/route.ts:101`
**Severidad:** 🔵 Bajo

**Descripción:**
Los emails de notificación de aceptación usan `.catch(err => console.error(...))` que es mejor que BUG-009 (silencio total), pero si Resend falla en producción, el vet_admin no recibe ninguna señal. El dueño/veterinario aceptado no sabe que fue aceptado hasta que entra manualmente al panel. En flujos críticos de onboarding, un fallo de email silencioso puede parecer un bug al usuario final.

**Código actual (mejor que BUG-009 pero mejorable):**
```typescript
await resend.emails.send({ ... })
  .catch((err: unknown) => { console.error('[care-request/accept] email:', err) })
```

**Fix sugerido:**
```typescript
const emailResult = await resend.emails.send({ ... }).catch((err: unknown) => {
  console.error('[care-request/accept] email failed:', err)
  return null
})
if (!emailResult) {
  // Opción A: registrar en tabla email_failures para reintento posterior
  // Opción B: incluir en la respuesta un flag warned: true para que el frontend muestre aviso
}
```

---

### BUG-021 — `auth/accept-invite/route.ts` — URL de login hardcodeada (extensión de BUG-011)

**Archivo:** `src/app/api/auth/accept-invite/route.ts`
**Línea:** 86
**Severidad:** 🔵 Bajo

**Descripción:**
Mismo patrón que BUG-011. El email de bienvenida usa `https://petfhans.com/auth/login` hardcodeado en lugar de `process.env.NEXT_PUBLIC_APP_URL`.

**Código problemático:**
```typescript
loginUrl: `https://petfhans.com/auth/login`,
```

**Fix:**
```typescript
const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'https://petfhans.com'
loginUrl: `${appUrl}/auth/login`,
```

---

## Estado de bugs por fase de desarrollo

| Bug | Estado | Bloquea |
|-----|--------|---------|
| BUG-001 | ✅ Resuelto Fase 1 | — |
| BUG-002 | ✅ Resuelto Fase 1 | — |
| BUG-003 | ✅ Resuelto Fase 2 | — |
| BUG-004 | ✅ Resuelto Fase 2 | — |
| BUG-005 | ✅ Resuelto Phase C | — |
| BUG-006 | ✅ Resuelto Fase 2 | — |
| BUG-007 | ✅ Resuelto Fase 3 | — |
| BUG-008 | ✅ Resuelto Fase 4 | — |
| BUG-009 | ✅ Resuelto Fase 4 | — |
| BUG-010 | ✅ Ya resuelto (orden presente en código) | — |
| BUG-011 | ✅ Resuelto Fase 5 | — |
| BUG-012 | ✅ Resuelto Fase 5 | — |
| BUG-013 | ✅ Resuelto Fase 1 | — |
| BUG-014 | ✅ Resuelto Fase 3 | — |
| BUG-015 | ✅ Resuelto Fase 2 | — |
| BUG-016 | ✅ Resuelto Fase 3 | — |
| BUG-018 | ✅ Resuelto Fase 4 | — |
| BUG-019 | ✅ Resuelto Fase 5 | — |
| BUG-020 | ❌ Abierto | Observabilidad |
| BUG-021 | ✅ Resuelto Fase 5 | — |

---

*v6 — 2026-04-26 — Todas las fases completadas (BUG-001 al BUG-021). Auditoría cerrada.*
